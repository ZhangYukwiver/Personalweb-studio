# 主页工坊 AI 功能实施方案

## 1. 目标

面向没有代码基础的个人主页用户，在现有编辑、预览和导出流程中加入：

- 必填信息：姓名、职业或方向、真实经历或项目素材。
- 选填信息：头像、主页用途、表达语气、技能、联系方式、所在地、项目图片与链接、社交链接、主题色。
- AI 整页完善和单字段润色，所有结果先对比、后确认应用。
- 清爽专业、创意作品集、极简履历三种固定模板。

第一版仅生成中文文字和标签，不生成图片，不让 AI 自由生成 HTML 或 CSS。

## 2. 实施步骤

### 2.1 模板切换闭环

1. 为草稿增加职业、事实素材、用途、语气和模板字段，并兼容现有本机草稿。
2. 在预览工具栏加入模板选择器，切换后立即更新预览和导出结果。
3. 让预览直接显示最终导出 HTML，保证预览与文件一致。
4. 三种模板共享同一份真实资料，只改变布局、配色和内容层级。
5. 完成桌面和手机预览后暂停，等待人工确认再进入 AI 接入。

### 2.2 AI 整页生成闭环

1. 在编辑器顶部增加 AI 助手区域，包括 API Key、必填资料、选填用途和语气、生成按钮。
2. API Key 使用密码输入框，仅保存在当前运行内存，关闭应用后清除。
3. 首次调用前提示用户确认资料会发送给 OpenAI。
4. 由 Tauri Rust 后端调用 OpenAI Responses API，使用严格结构化输出和 `store: false`。
5. 默认模型使用 `gpt-5.6-luna`，第一版不提供模型选择器。
6. 提示词要求只整理用户提供的事实，不得新增虚构经历、客户、成果或数字。
7. 生成标题、简介、技能和项目文字后打开对比面板，由用户逐项选择并应用。
8. 项目更新优先按标题匹配，其次按位置匹配；保留原项目 ID、图片和链接，不自动删除未匹配项目。
9. 对无效 Key、权限不足、限流、网络超时、模型拒绝和返回格式异常分别提供错误提示。

### 2.3 单字段优化闭环

1. 在一句话介绍、个人简介和项目介绍旁增加 AI 优化按钮。
2. 只发送当前字段及必要事实上下文，并显示原文与建议文本。
3. 用户确认后只替换目标字段；取消或请求失败时不修改草稿。
4. 请求期间禁用重复提交，并忽略已经过期的响应。

## 3. 接口与数据

- 增加 `TemplateId = 'professional' | 'creative' | 'resume'`。
- `PortfolioData` 增加 `role`、`sourceMaterial`、`purpose`、`tone` 和 `templateId`。
- 增加整页生成、字段润色、结构化建议和错误类型。
- AI 不得修改姓名、图片、邮箱、链接或主题色。
- 新增异步 Tauri 命令 `generate_ai_content`，接收操作类型、事实上下文和会话 API Key，返回经过 Rust 类型校验的建议数据。
- OpenAI 调用采用 Responses API 和 Structured Outputs。

## 4. 测试与验收

- 单元测试覆盖旧草稿兼容、三种模板导出、HTML 转义、安全链接、建议应用和项目合并。
- Rust 测试覆盖请求构造、结构化响应解析、拒绝、401、429 和异常响应。
- 测试使用本地服务，不写入真实 API Key。
- 运行 `npm run check`、`cargo test` 和 Tauri 构建检查。
- 分别预览三种模板的桌面与手机布局，检查溢出、空字段、长文本、图片缺失和导出一致性。
- 使用用户主动填写的 Key 做真实生成验收前再次确认，不读取或记录 Key。
- 每个垂直结果完成后提供真实 Preview，用户确认后才进入下一阶段。

## 5. 默认与边界

- 默认使用 OpenAI 官方 API、中文输出和 `gpt-5.6-luna`。
- 不建设自有后端，不承担统一调用费用。
- 不生成图片，不让 AI 生成任意 HTML 或 CSS。
- 浏览器开发模式用于验证编辑器和模板，真实 AI 调用只在 Tauri 桌面应用中启用。
- 不在代码、日志、草稿或错误信息中保存 API Key。
