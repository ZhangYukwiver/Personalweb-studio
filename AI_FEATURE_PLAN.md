# 主页工坊 AI 沉浸式网页方案

## 1. 目标

在现有清爽专业、创意作品集和极简履历之外，增加可保存、预览和导出的“AI 生成”模式：

- 输入最多三张参考截图和一段修改要求。
- 使用工作台中的姓名、简介、技能、项目、图片与联系方式，不复制参考站素材。
- AI 生成安全的 HTML 片段、CSS 和受控动效配置，不执行模型生成的 JavaScript。
- 受控动效支持滚动固定场景、项目媒体序列和 Canvas 粒子背景，可重复组合为沉浸式滚动叙事。
- 结果先作为候选预览，用户确认后才应用到当前主页。
- 第一版只支持桌面页面，固定模板原有的手机能力保持不变。

## 2. 分阶段交付

### 2.1 本地候选闭环

1. 增加生成设计、动效和截图附件类型，并兼容旧草稿。
2. 提供确定性的沉浸式本地候选，用于验证候选对比、应用、放弃和重新生成。
3. 页面内容通过数据槽填入，资料修改后预览与导出同步更新。
4. 生成模式强制桌面预览，手机预览按钮保持禁用。
5. 完成桌面 Preview 后暂停，等待人工确认再连接真实 API。

### 2.2 真实 AI 接入

1. 在 AI 面板加入只存在当前运行内存的 API Key。
2. 首次请求前展示将发送的资料与截图并请求确认。
3. 由 Tauri 后端调用 OpenAI 兼容接口；项目默认使用 AI.INPUT.IM 中国优化节点、Responses API 和 `gpt-5.6-sol`，也允许在当前会话配置 Responses 或 Chat Completions 的 Base URL 和模型名称。
4. 返回 HTML 片段、CSS、数据槽和动效配置；Rust 校验结构，前端继续执行安全过滤。
5. 对拒绝、401/403、429、超时、5xx、格式异常和安全校验失败提供明确错误，不自动重试产生额外费用。
6. Responses 使用 Structured Outputs；Chat Completions 使用标准消息格式并解析模型返回的 JSON，不自动重试或探测接口，避免重复计费。

## 3. 安全与导出

- HTML 仅允许语义布局标签和安全属性，移除脚本、事件属性、表单、iframe 和嵌入对象。
- CSS 使用解析器检查，拒绝外部 URL、`@import`、`@font-face` 和危险函数。
- 滚动显现、视差、吸附章节、跑马灯、固定场景、媒体序列和粒子背景由工作台可信运行时提供。
- 预览 iframe 使用 sandbox；单文件导出注入 CSP、减少动效降级并禁止网络请求。
- 最多三张截图，单张原文件不超过 4MB，压缩后总量不超过 8MB；HTML、CSS 和动效数量均设上限。
- API Key、兼容接口 Base URL 和模型名称只保存在当前运行内存，不进入草稿、日志、错误信息或生成文件。

## 4. 当前状态

- 已完成本地候选、数据槽、安全过滤、受控动效、候选确认、桌面预览和单文件导出闭环。
- 已完成固定模板回归、安全测试、生产构建和桌面浏览器 Preview。
- 已完成会话级 API Key、Responses / Chat Completions 接口格式、Base URL 与模型配置、截图压缩、发送前确认、返回解析和错误映射。
- 已完成可复用的固定滚动场景、真实项目媒体切换、粒子 Canvas、进度指示和减少动效降级；预览与单文件导出共用同一可信运行时。
- Rust 模拟测试已覆盖成功、拒绝、401/403、429、超时、5xx 和格式异常。
- 尚未由开发过程使用真实 API Key 发起请求；该动作继续受发送前人工确认门禁保护。
